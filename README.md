# 🌲 Piney (囤囤小兄许)

> SillyTavern 生态的全能角色卡工作站

## ✨ 功能特性

- **收藏管理**：多格式导入（PNG/JSON）、批量导入、标签系统、分类目录
- **编辑创作**：可视化角色卡编辑器、Prompt 医生、角色融合实验室
- **AI 智能**：自动解析元数据、AI 简介生成、智能标签、Prompt 诊断
- **数据分析**：角色 DNA 雷达图、Token 热力图、统计看板
- **趣味互动**：每日抽卡、成就系统、压箱底提醒

## 🛠️ 技术栈

| 层级 | 技术 |
|-----|------|
| 编程语言 | Rust + TypeScript |
| 桌面/移动端 | Tauri v2 |
| 前端框架 | Svelte + SvelteKit |
| UI 组件 | shadcn-svelte + TailwindCSS |
| 后端框架 | Axum |
| 数据库 | SeaORM + SQLite |

## 🚀 快速开始

### 前置要求

- [Rust](https://rustup.rs/) 1.75+
- [Node.js](https://nodejs.org/) 20+
- [pnpm](https://pnpm.io/) 或 npm

### 开发模式

```bash
# 1. 安装前端依赖
cd frontend
npm install

# 2. 启动前端开发服务器
npm run dev

# 3. 在另一个终端启动后端
cd ..
cargo run
```

访问 http://localhost:5173 查看前端，后端 API 运行在 http://localhost:9696

### Tauri 桌面应用

```bash
# 安装 Tauri CLI
cargo install tauri-cli

# 启动开发模式
cargo tauri dev
```

### Docker 部署

```bash
# 构建镜像
cd docker
docker-compose build

# 启动服务
docker-compose up -d
```

访问 http://localhost:9696

## 📁 项目结构

```
piney/
├── src/                    # Rust 后端源码
│   ├── api/               # Axum 路由和处理器
│   ├── db/                # 数据库层
│   ├── entities/          # SeaORM 实体
│   ├── services/          # 业务逻辑
│   ├── models/            # 请求/响应模型
│   └── utils/             # 工具函数
├── src-tauri/             # Tauri 配置
├── frontend/              # Svelte 前端
│   ├── src/
│   │   ├── lib/          # 工具库和 stores
│   │   ├── routes/       # 页面路由
│   │   └── components/   # 组件
│   └── static/           # 静态资源
├── migration/             # 数据库迁移
├── docker/                # Docker 配置
└── data/                  # 运行时数据
```

## ⚙️ 环境变量

| 变量名 | 说明 | 默认值 |
|-------|------|--------|
| ADMIN_USERNAME | 管理员用户名 | admin |
| ADMIN_PASSWORD | 管理员密码 | admin |
| PORT | 服务端口 | 9696 |
| DATA_DIR | 数据目录 | ./data |

## 📄 许可证

MIT License .
