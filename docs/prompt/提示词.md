请为我优化主页面的sidebar

1. 顶部Logo使用项目当前logo，你找一个合适的尺寸
2. Acme Inc更改为 "囤囤小兄许"，Enterprise移除（所以这里的布局要改一下，不是两行是一行了）
3. 移除点击 Acme Inc时的展开弹窗（即Teams）

4. 侧边栏使用Lucide图标库，侧边栏的具体内容为：

（冒号后面是要使用的Lucide图标）

- 看板:layout-dashboard
- 角色库:id-card
- 导入：upload
- AI实验室（分类）
- 角色融合:dna
- 世界书生成:book-user
- 系统（分类）
- 数据备份:database-backup
- 设置:settings

其中两个分类是当前sidebar中 "Platform的样式"，分类不使用图标

5. 底部部分

保留头像、用户名，移除邮箱。

点击展开的内容有：

- 头像和用户名，移除邮箱
- 深浅模式（当浅色模式时，显示文字为深色模式，图标moon;当深色模式时，显示文字为浅色模式，图标sun）
- 更改头像，图标为:image-plus
- 更改用户名，图标为:pencil-line
- 登出,图标为log-out

6. 页面内容

为了让我预览点击效果，创建所有的页面，页面内容只需要写当前页面名称就行，不用开始做。

例如看板页面写一个主页
角色库页面写一个角色库
导入页面写一个导入...

不需要做功能和美化